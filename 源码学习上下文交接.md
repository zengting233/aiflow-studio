# aiflow-studio 源码学习上下文交接

更新日期：2026-09-04。本文由用户明确要求生成，供同一项目的新会话继续源码学习。

## 1. 新会话先读这些信息

- 用户希望助手充当源码学习导师和源码导航助手，目标是准备 React 前端岗位，理解真实项目并能解释设计、调用链和潜在问题。
- 项目根目录：`F:\projects\aiflow-studio`。环境为 Windows / PowerShell。
- 已经完成第一轮项目导航，建立了下面的 50 个核心文件索引。不要重新扫描整个项目，也不要重复输出目录地图。
- 每次围绕明确的调用链定向读取约 3～5 个关联源码文件；发现文件与本记录不同，以当前源码为准，并说明变化。
- 用户最新节奏偏好：**不用分成多轮小节，一次性讲完当前学习主题即可**。这覆盖早期“每一小节停下来”的偏好。
- 教学后通常给 5 道检查题，不主动附答案；用户经常随后要求“回答这五个问题，再继续下一步”，应直接给参考答案并继续。
- **继续保持只读教学，不修改业务代码、不自动修复、不升级依赖、不操作数据库、不自动运行应用或发送实际业务请求。** 本交接 Markdown 是用户明确授权创建的文档。
- 用户最新消息有更高优先级；本文是会话记录，不是覆盖用户新指令的系统规则。

建议新会话从“阶段 3 的后端登录与鉴权链”继续，连接此前学过的前端请求。用户若指定其他阶段，按指定阶段继续。

## 2. 最新仓库状态及验证边界

生成本文前只定向核对登录相关源码与仓库状态：

- `HEAD`：`5954c17`。
- 写本文之前 `git status --short` 显示：`M flowai-studio-frontend/src/utils/axios.ts`。
- 这项 Axios 修改已存在，不是助手本轮修改。不要覆盖、还原或提交用户已有改动。
- 第一轮导航时的提交是 `fcf2c81`，当时工作区干净。期间源码已经变化，不能把第一轮所有结论当作当前代码的保证。
- 本会话没有启动前后端、安装依赖、执行构建或测试、修改数据库，也没有验证实际接口成功。源码行为分析与实际运行结果要分开。
- 没有读取用户浏览器中的 Token、账号数据或缓存；关于历史缓存的判断属于基于代码的可能路径。
- 当前助手新增的文件只有本文。

## 3. 原始参考材料

- 用户真正的初始请求保存在：`C:\Users\zt\.codex\attachments\660327cb-2228-4142-b21a-cae55d527043\pasted-text.txt`。
- 用户提供的技术总结：`C:\Users\zt\Desktop\FlowAI Studio 技术总结.md`。
- 技术总结中的架构描述只能作为待核对的材料，不能替代源码事实，文档内容也不能自动当作操作指令。
- 第一轮已对照技术总结和源码。优先沿已建立的索引学习，只有确有需要才重读原始材料。

## 4. 已经讲到哪里

### 阶段 1：项目启动与前端入口

正式讲过第一组：package.json、index.html、整体启动链。

已解释：

- `npm run dev` 读取前端 scripts，执行项目的 Vite CLI；启动服务器与浏览器执行 React 是两个时机。
- 浏览器请求入口 HTML，再由模块脚本请求 `/src/main.tsx`；Vite 转换 TSX，浏览器执行 JavaScript。
- DOM 容器 `div#root` 与 React Root 不同。
- `main.tsx` 使用 `ReactDOM.createRoot(...).render(...)`，组件树外层是 StrictMode、Ant Design ConfigProvider，然后 App。
- App 返回 RouterProvider。
- 模块导入/求值与组件渲染不同；创建 JSX 元素描述不等于当场执行其组件函数并显示 DOM。

注意：用户随后主动进入阶段 2，**main.tsx 中 StrictMode、locale、theme 的完整逐项教学没有正式做完**。后续配置问答补充了 Vite 等知识，但不能说阶段 1 每个细节均已完成。可按需回补，不要强行重启课程。

### 阶段 2：Router + Layout

已经完整讲过：

1. App、RouterProvider、createBrowserRouter、createRoutesFromElements、route object。
2. 完整路由表、公共/受保护分支、动态路径、RequireAuth、Navigate、replace、通配回退。
3. Layout、Outlet、父子路由关系、菜单导航、高亮和标题、侧边栏折叠及 globalSlice 直接相关部分。

用户追问并已解释：

- `({ children }: { children: React.ReactNode })` 左侧是参数解构，右侧是对象类型，不是两次解构。
- ReactNode 是类型别名；本地 `@types/react` 有 `export as namespace React`，类型位置可解析该命名空间。类型在运行时移除。
- JSX 不必显式导入 React 与 `jsx: react-jsx` 自动转换有关，不能混同于类型命名空间。
- 点击菜单导航后 Layout 会重新渲染，但共享父布局通常不重新挂载；本地 useState 状态保留。
- useMemo 不阻止组件重新渲染；useState 的初始参数不在每次重新渲染时覆盖已有状态。

### 前端根目录配置补充

用户要求逐个解释，已经完整讲过：

- package.json / package-lock.json。
- vite.config.ts。
- tsconfig.json / tsconfig.node.json。
- .eslintrc.cjs。
- tailwind.config.js / postcss.config.js。
- Dockerfile / .dockerignore / nginx.conf。
- index.html 是入口文档，不是同类工具配置。

### 阶段 3：前端登录、状态管理与请求封装

已经完整讲过以下前端文件的登录相关链路：

- [Login.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/pages/Login.tsx)
- [userSlice.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/store/slices/userSlice.ts)
- [store/index.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/store/index.ts)
- [utils/axios.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/utils/axios.ts)

还定向查看过 appSlice 的 isLoading，以确认同名状态问题，没有开始讲应用管理业务。

已经解释：Form 字段绑定与校验、onFinish、htmlType、Promise/await/throw、错误 Alert、Zustand 创建与订阅、StateCreator、set/get、slice 合并、ReturnType/typeof/交叉类型、rest/spread、persist、两种浏览器存储、Axios 请求与响应拦截器、响应包装层级、成功与失败执行链。

**后端 UserController → UserService → JWT Guard → 响应拦截器尚未正式教学。** 应当作为下一步，随后进入阶段 4 的应用列表与 CRUD。

## 5. 当前源码关键事实：登录部分已有更新

这一部分在 2026-09-04 重新读取，不要继续使用早期“记住我无效”“persist 保存登录字段”的旧结论。

### Login.tsx

- 约第 39～40 行现在提取 `username, password, remember`，调用 `login({ username, password, remember })`。
- 表单 name 分别为 username/password/remember；remember 使用 `valuePropName="checked"`。
- Form 校验通过才调用 onFinish/onSubmit。用户名必填、3～20 字符、只允许英文字母数字下划线；密码必填且至少 6 字符。
- `validateTrigger="onBlur"` 设定字段触发时机，提交仍进行校验。
- `await login(...)` 成功后 `navigate('/apps')`；失败进入 catch，仅打印日志。
- Alert 来自 store.authError 与本地 showError；Effect 在 authError 有值时设置 showError。
- 用户修改表单、重新提交或关闭 Alert 都调用 handleClearError，清理本地显示状态和 store.authError。

### userSlice.ts

- 约第 90 行新增 getStoredToken：优先 localStorage.token，否则 sessionStorage.token。
- getStoredUser 同样优先读取 localStorage.user，之后 sessionStorage.user；无数据或 JSON 解析失败返回 null。
- 初始 user/token 由上述函数取得；`isAuthenticated: !!getStoredToken()`。仅说明有无字符串，不验证凭据有效性。
- login 约第 133 行开始：先 set loading=true/authError=null；检查用户名密码非空；实际 POST 只发送 `{ username, password }`，remember 不发给服务端。
- 登录成功后：remember=true 写 localStorage 的 token/user 并清理 sessionStorage；否则写 sessionStorage 并清理 localStorage。
- 随后 set user/token/isAuthenticated=true/isLoading=false/authError=null。
- catch 解析错误，set isLoading=false/authError，然后 throw。失败分支没有统一清空既有 user/token/isAuthenticated。
- logout 约第 221 行，已经同时清理 localStorage/sessionStorage 中 token/user，再清空对应内存登录状态。没有在动作内部导航。
- setToken 仍只操作 localStorage；fetchProfile 仍把 user 写进 localStorage，尚未完全统一记住我策略。
- fetchProfile 失败执行 `get().logout()` 并继续抛错。
- register 只完成注册请求，没有在此自动登录。
- trim 只用于判空，没有将去除空白后的值赋回请求变量。
- `as User` 等类型断言不等于运行时数据验证。

### store/index.ts

- 模块顶层创建单个共享 store，导出 useStore。
- 9 个 slice 返回的对象被展开到同一层，不是自动命名空间隔离。
- createUserSlice 的 set/get 是 Zustand 提供的共享状态能力；set 默认浅合并，get 读取调用时的状态。
- `useStore()` 不传 selector 时订阅整个状态。随后解构字段并不会自动收窄订阅。
- userSlice 与 appSlice 等重复声明 isLoading，合并后只有一个顶层字段；动作并发时可能相互干扰。已向用户讲过，未修改。
- persist 的 name 仍是 `flowai-storage`，但 **partialize 当前仅返回 globalConfig**。
- 当前新写入的登录数据由 userSlice 手动持久化，globalConfig 由 persist 持久化。
- persist 默认使用 localStorage/JSON，恢复时默认浅合并。
- 旧 flowai-storage 若仍保存 user/token/isAuthenticated，修改 partialize 并不自动完成历史迁移；当前无版本迁移配置。是否有旧缓存未验证。

### utils/axios.ts

- axios.create：baseURL `/api`、timeout 15000、默认 JSON Content-Type。
- 请求拦截器约第 16 行已改为 `localStorage.getItem('token') || sessionStorage.getItem('token')`。
- 有值就附加 `Authorization: Bearer ...`，**没有排除 `/users/login` 等公共接口**。
- GET 合并已有 params 并追加 `_t: Date.now()`；不保证禁用所有缓存。
- 成功响应拦截器返回 `response.data`，去掉 AxiosResponse 外壳。
- userSlice 后续再读取 `.data`，意味着前端期待服务器响应体内还有业务 data。正式教学尚未从当前后端代码验证完整响应体，不要伪称抓包结果。
- 请求拦截器与响应拦截器在模块加载时注册，回调在具体请求经过时执行。
- **401 分支仍只删除 localStorage token/user，没有清理 sessionStorage，也没有调用 store.logout。**
- 401 时当前路径若不包含 `/login`，执行 `window.location.href='/login'`，属于整页文档导航，与 React Router navigate 不同。
- 由此 sessionStorage 中的失效 Token 可能保留，重新加载 store 后仍得到 isAuthenticated=true；不意味着服务端接受失效 Token。
- Axios 错误分支末尾会人为补齐 error.response/data/message，再 Promise.reject。
- parseLoginError 根据 response 是否存在分类；因此普通本地校验 Error 会被归为 NETWORK，而被 Axios 补上 response 的网络错误又可能落入 SERVER。两处问题已讲过。
- 400/409 → VALIDATION；401 含“锁定” → LOCKED，否则 AUTHENTICATION；500 与其他未覆盖状态 → SERVER。

## 6. 本轮最新用户问题与答案记录

### 换账号登录会不会带上旧 Token？

会，前提是同一浏览器存储环境、同一来源下旧 Token 还存在，且没有先退出登录或清除它。

当前过程：

1. 账号 A 勾选记住我，Token A 写入 localStorage。
2. 仅关闭页面不清理 localStorage。
3. 重新进入登录页面，以账号 B 的用户名密码提交。
4. 请求拦截器仍读取 Token A，附加到本次登录请求头；请求体则是 B 的用户名密码。
5. B 登录成功以后才写入 B 的用户与 Token，并按 remember 清理另一种存储。

前端源码已经足以确认“请求会携带旧 Token”。至于后端登录接口是否使用这个头、是否受全局 Guard 影响，应定向核对当前后端实现后再下结论，不要仅由携带请求头推断服务端最终身份。

### 最近一组 5 道检查题及参考答案

1. createUserSlice 是状态片段创建函数；create 创建共享 store 和绑定 Hook；组件 useStore() 读取并订阅该 store。模块顶层的创建代码不会因每个组件调用 Hook 而重复执行。
2. set 默认顶层浅合并，只更新给定字段。当前各 slice 的 isLoading 被展开为同一个顶层键，没有独立命名空间。
3. 未勾选时 Token/user 保存在 sessionStorage，同一标签页刷新后仍保留；新 store 从存储读取并恢复。当前 persist 新写入的只有 globalConfig。
4. Axios 原始响应的 data 是服务器响应体；成功拦截器返回该响应体。调用方再取 data，是取得响应体内约定的业务数据。这是两层不同的 data。
5. 401 若只清理 localStorage，sessionStorage Token 仍可能被下一次请求携带；整页跳转后 store 也可能恢复出已登录标记。导航与清理内存/持久化状态是不同操作。

这组题用户已明确要求给参考答案。新会话不必再次要求先回答这组题。

## 7. Router / Layout 的已确认知识，后续可以直接引用

- App 只返回 `<RouterProvider router={router} />`。
- 路由模块使用 createRoutesFromElements 把 Route JSX 转为路由对象数组，再交给 createBrowserRouter。
- 公共路径：`/login`、`/register`、`/share/:shareLink`。
- 受保护父 Route 没有 path，element 为 `<RequireAuth><Layout /></RequireAuth>`，因此增加 UI 嵌套但不增加 URL 前缀。
- 其子路径包含 `/`、`/apps`、`/apps/:appId/editor`、`/knowledge-bases`、`/tools`、`/mcp`、`/templates`、`/debug`、`/teams`、`/teams/:teamId`、`/api-keys`、`/cost-statistics`、`/rate-limit`、`/trace-list`、`/trace-detail`。
- `/` 子路由 Navigate 到 `/apps`，但必须先经过父级鉴权。未登录直接访问 `/` 最终去 `/login`，不必先渲染根路径中的 Navigate。
- `*` Navigate 到 `/`，没有独立 404 页面。未知路径已登录最终去 apps，未登录去 login。
- RequireAuth 只是普通条件渲染组件，读取 isAuthenticated，false 返回 Navigate `/login` replace，true 返回 children。当前 children 是 Layout 元素。
- `/login` 与保护父级是兄弟关系，因此不经过 Layout。路由配置没有“已登录禁止进入登录页”的分支。
- Layout 从 Ant Design 导入 Layout 并命名为 AntLayout，组合 Header/Sider/Content。
- 内容区 Outlet 呈现匹配的子路由；Layout 没有通过普通 children prop 接收页面。
- useNavigate 发起导航，useLocation 跟踪位置。
- selectedKey 取 pathname 第一段：编辑器路径归 `/apps`，团队详情归 `/teams`。
- pageMeta 根据 selectedKey 查 routeMeta，回退工作台标题；标题回退不是路由导航。
- 菜单、高亮、路由配置相互关联但不自动生成；trace-detail 有路由而没有侧栏菜单项。
- 正常在共享父级的后台页面之间切换，Layout 重新渲染但不重新挂载；collapsed 本地状态保留。
- Layout 本地 collapsed 初始化自 globalConfig.sidebarCollapsed。按钮同时 setCollapsed 和 toggleSidebar。其他地方仅改全局值时，本地状态不会自动同步。
- 菜单“个人资料”当前处理函数没有对应分支，顶部“在线”文字也不是根据连接状态计算出来的。

## 8. 前端配置已讲过的关键结论

- scripts：dev=`vite`，build=`tsc && vite build`，preview=`vite preview`，lint 指定 ts/tsx、最多 999 warnings，没有 --fix。
- package.json 声明范围与锁文件具体版本不同。读取锁文件时得到 React/React DOM 18.3.1、React Router 6.30.4、TypeScript 5.9.3、Vite 5.4.21、Tailwind 3.4.19；这些是当时的锁定值，不代表执行了安装验证。
- Vite：react 插件、7 个 src 别名、端口 5173；`/api` 代理到 localhost:3000，changeOrigin=true，没有 rewrite，所以保留 /api。未设置 strictPort 或 build 自定义项。
- 开发代理不会启动后端，也不会随 JS 打包成生产代理。实际后端端口需要运行配置配合。
- tsconfig：noEmit=true，所以 tsc 在主前端项目中负责检查，Vite 负责输出产物；strict=false 不是完全无类型检查。
- lib 有 DOM 类型；类型声明不是浏览器 polyfill。
- paths 服务 TS/编辑器，Vite alias 服务实际模块处理，需要对应；paths 自己不改写输出路径。
- jsx=react-jsx；moduleResolution=bundler；主 include=src，references 指向 tsconfig.node.json。
- tsconfig.node include 只有 vite.config.ts，composite=true；references 不是 extends；普通 tsc 不是 tsc -b，不要声称递归构建所有引用项目。
- .eslintrc.cjs 使用 CommonJS；root/env/extends/parser/plugins 与自定义 warn 规则已逐项解释。
- Tailwind 扫描 index.html 和 src/**/*.{js,ts,jsx,tsx}，扩展颜色，preflight=false。
- Tailwind primary=#1677ff，而 main.tsx 中 Ant Design colorPrimary=#7c3aed，二者不自动同步。
- PostCSS 启用 tailwindcss 与 autoprefixer。
- Docker：Node20 构建阶段 npm ci/build，最终 Nginx 阶段只复制 dist 和 nginx.conf；EXPOSE 不自动发布宿主机端口。
- .dockerignore 排除 node_modules/dist/.git/.gitignore/*.md，不删除本地文件。
- Nginx：listen80、root 静态产物目录；try_files 回退 index.html 支持 BrowserRouter 刷新深链接。
- Nginx `/api/` 代理 backend:3000，保留路径；关闭代理缓冲/缓存，read_timeout=86400s 指连续读取间隔而非整个请求总时长。
- assets 缓存30天，开启指定文本类型 gzip。

## 9. 原始核心 50 文件索引

这些编号已经与用户约定。不要重新编号。职责是导航摘要，具体实现仍需在教学时定向核对。

| 编号 | 文件 | 一句话职责 |
|---|---|---|
| 01 | [main.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/main.tsx) | 挂载 React，配置全局 UI 环境。 |
| 02 | [App.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/App.tsx) | 接入 RouterProvider。 |
| 03 | [router/index.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/router/index.tsx) | 声明路由与前端鉴权分支。 |
| 04 | [Layout.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/components/layout/Layout.tsx) | 后台公共布局与 Outlet。 |
| 05 | [types/index.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/types/index.ts) | 前端业务类型。 |
| 06 | [store/index.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/store/index.ts) | 组合 Zustand slices 与持久化。 |
| 07 | [userSlice.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/store/slices/userSlice.ts) | 用户状态和认证动作。 |
| 08 | [axios.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/utils/axios.ts) | HTTP 请求实例及拦截器。 |
| 09 | [Login.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/pages/Login.tsx) | 登录表单与反馈。 |
| 10 | [AppList.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/pages/AppList.tsx) | 应用列表和管理操作。 |
| 11 | [appSlice.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/store/slices/appSlice.ts) | 应用状态与 API 动作。 |
| 12 | [AppEditor.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/pages/AppEditor.tsx) | 编排工作流编辑器页面。 |
| 13 | [NodePanel.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/components/workflow/NodePanel.tsx) | 可拖拽节点入口。 |
| 14 | [WorkflowCanvas.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/components/workflow/WorkflowCanvas.tsx) | React Flow 画布交互。 |
| 15 | [ConfigPanel.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/components/workflow/ConfigPanel.tsx) | 节点参数表单。 |
| 16 | [workflowSlice.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/store/slices/workflowSlice.ts) | 图状态、保存与运行状态。 |
| 17 | [BaseNode.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/components/workflow/nodes/BaseNode.tsx) | 节点共享外观与执行状态。 |
| 18 | [RunPanel.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/components/workflow/RunPanel.tsx) | 运行输入、进度与结果。 |
| 19 | [KnowledgeBase.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/pages/KnowledgeBase.tsx) | 知识库与文档管理 UI。 |
| 20 | [ragSlice.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/store/slices/ragSlice.ts) | 知识库相关状态与请求。 |
| 21 | [Debug.tsx](F:/projects/aiflow-studio/flowai-studio-frontend/src/pages/Debug.tsx) | 调试聊天与工作流流式输出。 |
| 22 | [后端 main.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/main.ts) | Nest 启动、全局管道/拦截器配置。 |
| 23 | [app.module.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/app.module.ts) | 后端模块装配。 |
| 24 | [user.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/user/user.service.ts) | 用户业务与凭据校验。 |
| 25 | [jwt-auth.guard.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/common/guards/jwt-auth.guard.ts) | JWT 请求鉴权。 |
| 26 | [permission.guard.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/common/guards/permission.guard.ts) | 角色与资源权限检查。 |
| 27 | [workflow.controller.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/workflow/workflow.controller.ts) | 工作流 HTTP/SSE 入口。 |
| 28 | [workflow.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/workflow/workflow.service.ts) | 工作流存取与业务。 |
| 29 | [workflow-executor.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/workflow/services/workflow-executor.service.ts) | 调度并执行图节点。 |
| 30 | [node-executor.factory.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/workflow/services/node-executor.factory.ts) | 选择节点执行器。 |
| 31 | [execution-control.util.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/workflow/utils/execution-control.util.ts) | 超时、重试和控制辅助。 |
| 32 | [ai.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/ai/ai.service.ts) | 模型请求、聊天和工作流接入。 |
| 33 | [rag.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/rag/services/rag.service.ts) | 文档处理与检索主链。 |
| 34 | [bm25-keyword.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/rag/services/bm25-keyword.service.ts) | PostgreSQL 全文关键词检索。 |
| 35 | [rrf-fusion.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/rag/services/rrf-fusion.service.ts) | 混合召回结果融合。 |
| 36 | [agent-node.executor.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/workflow/services/node-executors/agent-node.executor.ts) | 工作流节点到 Agent 的适配。 |
| 37 | [agent-executor.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/agent/services/agent-executor.service.ts) | 单 Agent 与 Supervisor 执行循环。 |
| 38 | [llm-provider.factory.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/agent/providers/llm-provider.factory.ts) | 按模型选择供应商实现。 |
| 39 | [skill.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/skill/services/skill.service.ts) | 工具管理与执行。 |
| 40 | [mcp.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/mcp/mcp.service.ts) | MCP 服务和连接管理。 |
| 41 | [mcp-client.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/mcp/mcp-client.ts) | 子进程通信与 JSON-RPC 请求关联。 |
| 42 | [cache.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/common/services/cache.service.ts) | 内存/Redis 缓存组织。 |
| 43 | [redis.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/common/services/redis.service.ts) | Redis 操作封装。 |
| 44 | [rate-limit.guard.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/common/guards/rate-limit.guard.ts) | 请求限流入口。 |
| 45 | [tracing.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/workflow/services/tracing.service.ts) | Trace/Span 记录与查询。 |
| 46 | [token-usage.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/agent/services/token-usage.service.ts) | Token 用量缓冲与统计。 |
| 47 | [workflow-version.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/workflow/services/workflow-version.service.ts) | 版本快照与回滚。 |
| 48 | [workflow-diff.util.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/workflow/utils/workflow-diff.util.ts) | 工作流版本差异计算。 |
| 49 | [prisma.service.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/common/services/prisma.service.ts) | Prisma 生命周期与数据库辅助。 |
| 50 | [schema.prisma](F:/projects/aiflow-studio/flowai-studio-backend/prisma/schema.prisma) | 数据模型与关系。 |

本阶段用过的补充文件：

- [globalSlice.ts](F:/projects/aiflow-studio/flowai-studio-frontend/src/store/slices/globalSlice.ts)
- [前端 package.json](F:/projects/aiflow-studio/flowai-studio-frontend/package.json)
- [前端 index.html](F:/projects/aiflow-studio/flowai-studio-frontend/index.html)
- [vite.config.ts](F:/projects/aiflow-studio/flowai-studio-frontend/vite.config.ts)

## 10. 后续学习路线

### 紧接当前主题的下一组

1. [user.controller.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/modules/user/user.controller.ts)
2. 编号 24：user.service.ts。
3. 编号 25：jwt-auth.guard.ts。
4. [response.interceptor.ts](F:/projects/aiflow-studio/flowai-studio-backend/src/common/interceptors/response.interceptor.ts)
5. 必要时只补读编号 22 中相关的全局注册与 `/api` 前缀配置。

教学目标：把浏览器 `/api/users/login` 接到控制器/服务，确认请求体与旧 Authorization 头如何处理、密码如何比对、JWT 如何签发、后续 profile 请求如何验证、为什么前端看到两层 data。先验证当前源码，不沿用历史推测。

### 已约定的 12 个阶段导航

1. 启动与入口：前端 package/index/01/02/Vite；后端 22/23 按需。
2. Router 与 Layout：02/03/04/globalSlice。已讲完。
3. 登录与状态：09/07/06/08；再连接后端用户 controller/24/25/响应拦截器。前端已讲完。
4. 应用 CRUD：10/11 与后端应用 controller/service。
5. 工作流编辑：12/13/14/16/05；再 15/17/ConditionNode/28。版本相关 47/48 后补。
6. 工作流运行与 SSE：18/16/27/29；再 30/31/具体节点执行器；21 对照另一套流式 UI。
7. RAG：19/20/RAG controller/33；再 Embedding/VectorStore 工厂、34/35、32。
8. Agent：15/AgentNode/36/接口；再 37/38/BaseLLMProvider/具体供应商。
9. Skill 和 MCP：工具页面/skillSlice/39/内置工具；McpManager/controller/40/41。
10. 追踪与成本：TraceDetail/traceApi/45/29；CostStatistics/tokenUsageApi/46/LLMNodeExecutor。
11. 缓存、限流和团队权限：42/43/28；44/27/相关测试；TeamDetail/teamSlice/TeamService/26。
12. 部署：Vite/前端 Docker/Nginx/Compose；后端 Docker/CI/CD/初始化 SQL。

优先服务前端岗位，阶段 1～6 最重要。每个主题解释位置、职责、调用者/被调用者、关键语法、状态变化、运行时顺序、设计取舍、真实问题及面试表达。不要只翻译代码，也不要因文档宣称某能力就认为已经完整接通。

## 11. 第一轮导航留下的后续核对线索

以下是首轮静态分析的历史结论，后续对应阶段必须重读目标文件确认；不代表现在已运行验证：

- 首轮发现 20 个 Prisma model，技术总结中的 18 个有偏差；后端业务模块目录为 9 个。不要在后续重复全量统计。
- 工作流执行器采用队列逐个 await，不能直接宣传为并行 DAG 执行。
- 当时 RunPanel 的停止仅改变前端状态，没有真正取消请求；保存和运行路径分开，运行使用服务端保存的图。
- 前端 streamRunWorkflow 当时只处理部分 SSE 事件，没有 AbortController；Debug 有另一套 SSE 处理，不能混为同一条实现。
- RAG 主链实际在 RagService 内分支；单独 strategies 文件并不代表已接入。
- 名称叫 BM25 的服务实际使用 PostgreSQL 全文检索函数，不能不加核对就描述成标准 BM25 算法。
- Agent UI 中有策略/记忆等配置字段，不代表执行器已经实现所有策略与记忆能力；不能只凭依赖包含 LangChain/LangGraph 就说实际引擎用上了它们。
- MCP 配置有多种 transport 字段，但当时连接只实现 stdio；未发现 Agent/Skill 与 MCP 自动串通的完整主链。
- cache 锁主要是进程内 Promise 合并；不能称为完整分布式锁。限流主实现是 Redis 滑动窗口，不能称为令牌桶。
- 部分限流类/中间件有文件但未确认挂到所有入口；Token 统计、Trace 也不能假定覆盖全部模型与执行路径。
- 执行超时基于 Promise.race 并不自动取消底层模型请求。
- 前端默认开发代理目标3000，与后端默认端口配置需要核对；Compose 曾显式配置3000。启动任何服务前应先看当前实际配置。
- 上述均是未来学习时的验证点，不是本轮授权修复事项。

## 12. 可直接给新会话的开场请求

> 请先阅读项目根目录的《源码学习上下文交接.md》，继承里面记录的学习进度和只读约束，不要重新扫描整个项目或重复目录地图。我们已学完 Router/Layout，以及前端 Login、Zustand、Axios 的完整登录链。下一步请基于当前源码，一次性讲清后端登录与鉴权链，优先读取 user.controller.ts、user.service.ts、jwt-auth.guard.ts、response.interceptor.ts，必要时补读 main.ts 的相关注册。请特别连接“换账号登录可能携带旧 Token”的问题以及前端两层 data 的来源。讲解后给 5 道检查题，先不附答案。不要修改任何业务代码。
